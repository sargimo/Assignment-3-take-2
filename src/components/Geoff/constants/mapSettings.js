const mapSettings = {
    zoom: 13,
    minZoom: 10,
    maxZoom: 25
}

const regularMarker = {
    url: "../../../assets/geoff/geoff-map-marker.png",
}

export { mapSettings };
export { regularMarker };